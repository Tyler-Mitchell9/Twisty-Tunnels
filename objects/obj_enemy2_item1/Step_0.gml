/// @description Arrow moement 

// Move the arrow in the direction it's facing
var arrowSpeed = 10;  // Adjust speed as necessary
x += lengthdir_x(arrowSpeed, obj_enemy2.arrowDirection);
y += lengthdir_y(arrowSpeed, obj_enemy2.arrowDirection);